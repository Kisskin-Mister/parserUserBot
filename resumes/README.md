# resumes/

This directory is for personal resume PDFs. Resume files contain PII and are
**not tracked in git** (see `.gitignore`).

To use the bot's `apply` command, place your resume here and set `RESUME_PATH`
in `.env`, e.g.:

    RESUME_PATH=resumes/resume_main.pdf
