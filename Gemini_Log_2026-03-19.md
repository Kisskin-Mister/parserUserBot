# Лог Gemini - 19.03.2026

## Выполненные задачи
- Пересмотрена структура проекта и логика работы агентов.
- В `database.py` добавлен метод `delete_vacancy` для очистки базы от мусора.
- В `mcp_server.py` добавлены команды:
  - `delete`: удаление нерелевантных постов.
  - `processed`: пометка вакансии как обработанной.
  - `post`: публикация отформатированной вакансии в канал.
- Созданы файлы навыков:
  - `JOB_HUNTER_SKILL.md`: логика фильтрации, форматирования и связи с рекрутерами.
  - `IT_CURATOR_SKILL.md`: тихая обработка новостей.
- Обновлен стиль общения Codex: маленькие буквы, зумерский вайб, дефисы вместо тире, отсутствие точек, новые строки для предложений.
- В `main.py` внедрена фильтрация по `keywords.txt` и `negative_keywords.txt`.
- Изменен триггер передачи управления в `AGENT_TASK.md` на явную команду активации скилла.
- В `JOB_HUNTER_SKILL.md` добавлена критическая инструкция: Codex ОБЯЗАН читать файл скилла перед началом работы.
- Подготовлен промпт для настройки OpenClaw.
- Обновлены инструкции для Sentinel (`AGENT_TASK.md`) и общий гайд интеграции (`INSTRUCTION.md`).

## Наблюдения
- Проблема с повторными сводками каждые полчаса должна исчезнуть, так как теперь агент имеет инструменты для изменения статуса вакансий в БД.
- Классификация в `main.py` оставлена умеренно строгой, основной упор сделан на фильтрацию агентом Codex.

## Следующие шаги
- Проверить работу Codex с новыми навыками при поступлении следующей вакансии.
- Уточнить список "обширных кейвордов" у пользователя при необходимости.
